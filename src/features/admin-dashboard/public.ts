/**
 * Public admin-dashboard surface for other features (e.g. order-status
 * invalidation). Prefer this over deep imports into admin-dashboard/api.
 */
export { adminDashboardKpisQueryKey } from "./api/queryKeys";
