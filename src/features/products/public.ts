/**
 * Public products surface for other features (e.g. review invalidation).
 * Prefer this over deep imports into products/api.
 */
export { productQueryKey, productQueryKeyRoot } from "./api/queryKeys";
